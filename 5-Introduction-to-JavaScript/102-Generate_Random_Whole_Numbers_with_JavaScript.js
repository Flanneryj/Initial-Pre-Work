/* Using (Math.floor) in company with (Math.random) I am able to round a number down to the nearest whole number
I learned how to generate and return a random whole number between 0 and 9
*/
var randomNumberBetween0and19 = Math.floor(Math.random() * 20);

function randomWholeNum() {

  // Only change code below this line.

  return Math.floor(Math.random() * 10);
}
