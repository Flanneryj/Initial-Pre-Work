/* Math.random allows you to generate a random decimal number between 0 (inclusive) and not quite up to 1 (exclusive)
All function calls will be resolved before the return executes, so we can return the value of the Math.random() function
I learned how to generate random fractions using (Math.random)
*/

function randomFraction() {

  // Only change code below this line.

  return Math.random();

  // Only change code above this line.
}
