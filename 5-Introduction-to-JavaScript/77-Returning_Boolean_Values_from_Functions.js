/* Sometimes you can shorten boolen if else statments with comparison operators
Example
function isEqual(a,b) {
  if (a === b) {
    return true;
  } else {
    return false;
  }

comparison operators example
function isEqual(a,b) {
  return a === b;
}
*/

function isLess(a, b) {
  // Fix this code
return a <= b;
}

// Change these values to test
isLess(10, 15);
