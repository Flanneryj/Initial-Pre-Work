/* Using the arrow function syntax I was able to declare an anonymous function in a shorthand context
We often don't need to name our functions, especially when passing a function as an argument to another function
* considered inline functions.
*/

// new code
const magic = () => new Date()
